# Contributing

Keep changes small, observable, and covered by tests. Use semantic controls, preserve keyboard behavior, and avoid introducing network or credential dependencies into the local test suite.

Before opening a pull request, run lint, typecheck, tests, and the production build. Review the final diff for generated files, debug output, and unrelated cleanup.
